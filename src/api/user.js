import { request } from '../utils/request'

export default {
    // for example
    // getUserInfo: (data) => {
    //     return request.post('thaier/user/info', data)
    // }
    // 页面
    // this.$api.user.getUserInfo({id:'df222222'})
}